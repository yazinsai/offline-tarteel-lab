kind: model_only
hypothesis: "Deterministic tier-2 placeholder while phoneme Modal jobs train out-of-band"
owner: ""
constraints:
  max_onnx_mb: 200
expected_metrics:
  recall_delta_pp: null
notes: >
  Mirrors experiments/smoke tier-2 ABI for autopilot key model.fastconformer_phoneme_smoke.
  Does not bundle ONNX or checkpoints; training is triggered separately via Modal when allowed.

streaming_params:
  FIRST_MATCH_THRESHOLD: 0.0
  chunk_seconds: 0.30
  window_lock_reference_chunk_seconds: 0.25
