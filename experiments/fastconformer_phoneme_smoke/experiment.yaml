kind: model_only
hypothesis: "Tier-2 shim for phoneme FastConformer training evaluation; delegates to smoke lock logic"
owner: ""
constraints:
  max_onnx_mb: 200
expected_metrics:
  recall_delta_pp: null
notes: >
  Local reviewable stand-in until real NeMo weights exist.
  Reuses smoke first-verse / streaming ABI; tags outputs for phoneme pipeline tracking.
  Modal training remains in training/train_fastconformer_phoneme_modal.py (not invoked as part of predict).

streaming_params:
  FIRST_MATCH_THRESHOLD: 0.0
  chunk_seconds: 0.30
  window_lock_reference_chunk_seconds: 0.25
