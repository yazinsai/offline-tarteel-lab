kind: model_only
hypothesis: "Tier-2 ABI placeholder for phoneme FastConformer autopilot tracks (deterministic shim)"
owner: ""
constraints:
  max_onnx_mb: 200
expected_metrics:
  recall_delta_pp: null
notes: >
  Does not ship weights or ONNX in-repo. Reuses experiments/smoke deterministic predict() so
  bounded local evaluation can filter with --experiment fastconformer_phoneme_smoke while Modal
  training remains an out-of-band artifact.

streaming_params:
  FIRST_MATCH_THRESHOLD: 0.0
  chunk_seconds: 0.30
  window_lock_reference_chunk_seconds: 0.25
