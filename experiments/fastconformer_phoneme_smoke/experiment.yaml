kind: model_only
hypothesis: "Local tier-2 shim for FastConformer phoneme smoke path; no committed weights"
owner: ""
constraints:
  max_onnx_mb: 200
expected_metrics:
  recall_delta_pp: null
notes: >
  Deterministic first-verse lock compatible with tier2_local predict(audio_path).
  Uses the same streaming gate as experiments/smoke; transcript tags the phoneme
  evaluation lane for benchmark manifests. Modal training stays optional via queue payload.

streaming_params:
  FIRST_MATCH_THRESHOLD: 0.0
  chunk_seconds: 0.30
  window_lock_reference_chunk_seconds: 0.25
