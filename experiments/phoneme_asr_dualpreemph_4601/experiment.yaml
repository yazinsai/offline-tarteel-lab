kind: model_only
hypothesis: >
  Triplicate joint02 ONNX + matcher path with three deterministic pre-emphasis coefficients
  (defaults 0.97, 0.915, 0.865; PHONEME_DUAL_PREEMPH), unioning bounded CTC beam hypotheses
  before matching. Optional second CMVN branch via PHONEME_DUAL_CMVN (rms_mean) for env-only
  tweaks. Up to three ONNX forward passes per clip (mean CMVN only by default).
owner: ""
constraints:
  max_onnx_mb: 200
  forward_pass_cap: 3
expected_metrics:
  recall_delta_pp: null
notes: >
  escalate.non_smoke.model_only.46.01 acoustic pool; ONNX forward passes capped at two with shared CPU provider.
