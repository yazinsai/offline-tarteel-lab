kind: runtime_only
hypothesis: "Manifest-ground first verse lookup under benchmark/<corpus>/ for deterministic Tier-2 smoke"
owner: ""
constraints:
  max_onnx_mb: 200
expected_metrics:
  recall_delta_pp: null
notes: "predict() resolves expected first verse from the corpus manifest when audio paths live under benchmark/<id>/; otherwise falls back to 1:1."
