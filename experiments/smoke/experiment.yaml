kind: runtime_only
hypothesis: "Standalone smoke experiment for lab pipeline wiring"
owner: ""
constraints:
  max_onnx_mb: 200
expected_metrics:
  recall_delta_pp: null
notes: "Returns deterministic verse for smoke testing only"
