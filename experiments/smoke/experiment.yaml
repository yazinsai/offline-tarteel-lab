kind: runtime_only
hypothesis: "Manifest-aligned first-verse stub for streaming Tier-2 checks"
owner: ""
constraints:
  max_onnx_mb: 200
expected_metrics:
  recall_delta_pp: null
notes: "Reads expected first verse from corpus manifest next to audio; deterministic, no model weights"
