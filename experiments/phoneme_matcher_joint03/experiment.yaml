kind: joint_model_runtime
hypothesis: >
  Shipped phoneme ONNX + joint02 matcher, with expanded CTC prefix beam (beam 10, top-11
  symbols/frame, up to 6 string hypotheses) versus joint02 defaults (6/8/4). Matcher still
  selects argmax phoneme-graph score among hypotheses — not the rejected hypfuse LP blend.
owner: ""
constraints:
  max_onnx_mb: 200
  decode_cost_vs_joint02: "~1.3–1.8× ONNX frame loop (beam expansion only)"
expected_metrics:
  recall_delta_pp: null
notes: >
  Model-only autonomy probe targeting ASR decode search; inherits TOP_K 18, TOP_SURAHS 32,
  FRAGMENT_BLEND 0.82, MAX_SPAN 6 from joint02 code paths.
