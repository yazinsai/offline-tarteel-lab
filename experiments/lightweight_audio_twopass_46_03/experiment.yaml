kind: model_only
hypothesis: >
  Same ONNX and matcher head as phoneme_matcher_joint02, but multiple Mel front-ends
  (pre-emphasis 0.97/0.88/0.0 and f_max 8000/7600) to generate a merged CTC hypothesis pool
  before Quran matching — acoustic two-pass decode diversity, not standalone matcher rerank.
owner: ""
constraints:
  max_onnx_mb: 200
expected_metrics:
  recall_delta_pp: null
notes: >
  Autopilot task escalate.non_smoke.model_only.46.03 (shard 3). CPU/time bounded by small
  fixed variant list and merged hypothesis cap.
