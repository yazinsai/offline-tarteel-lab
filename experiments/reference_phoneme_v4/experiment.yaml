kind: joint_model_runtime
hypothesis: >
  Reference-shipped pipeline: ONNX log-probs, prefix CTC beam, sequence rescoring (ctc-rescore.ts),
  greedy-order tie-break, then Quran phoneme DB + Levenshtein/span search with bounded trigram shortlist.
  ONNX fetched to user cache; no committed weights.
owner: ""
constraints:
  max_onnx_mb: 200
expected_metrics:
  recall_delta_pp: null
notes: >
  Uses release ONNX sha256 78279e112f2762312b1412880d35f21f423bd117ee9222b82b8b9782c81bfaf9.
  Decode: beam + CTC forward acoustic score (reference RN) before verse matching.
  Matcher defaults widened toward phoneme_matcher_joint02-style shortlist for bounded full-corpus tier-2.
