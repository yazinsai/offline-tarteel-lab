kind: joint_model_runtime
hypothesis: >
  Reference FIFO task baseline.reference_shipped_fastconformer_v4_tlog.41: same shipped ONNX and
  joint02 matcher/search as phoneme_matcher_joint02, with wider default CTC prefix beam (10-wide,
  top-10 symbols/frame, 8 hypotheses) for additional decode search; matcher still picks best
  Levenshtein hit across hypotheses.
owner: ""
constraints:
  max_onnx_mb: 200
expected_metrics:
  recall_delta_pp: null
notes: >
  Decode/search knob bump only (defaults in run.py); tune via PHONEME_CTC_BEAM_WIDTH,
  PHONEME_CTC_TOP_SYMBOLS, PHONEME_MAX_HYPOTHESES.
