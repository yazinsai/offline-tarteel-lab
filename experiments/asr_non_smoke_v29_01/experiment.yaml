kind: model_only
hypothesis: >
  ASR-side preemphasis-ensemble ONNX posteriors plus wider CTC beam before the existing
  joint02 matcher harness; probes non-smoke audio→phoneme fidelity without corpus metadata use.
owner: ""
constraints:
  max_onnx_mb: 200
expected_metrics:
  recall_delta_pp: null
notes: >
  Task escalate.non_smoke.model_only.29.01 shard 3. Defaults widen beam/top-symbols/max-hypotheses
  versus joint02-only env when unset.
